---
title: "Evolution Text Analysis"
author:
  - name: Jesse Powell
date: "'r Sys.Date()'"
output: radix::radix_article
---

```{r}
#first we need to import our data using readtext.

library(readtext)

evolution_data_txt <-
  readtext("~/anaconda3/RStudioExperiments/Evolution_txt")

evolution_data_txt
```

```{r}
#We need to then convert our data to a Vcorpus object

library(tm)

evolution_corpus = Corpus(VectorSource(evolution_data_txt$text))

evolution_corpus
```

```{r}
#Our Vcorpus object needs to be preprocessed.

evolution_corpus = tm_map(evolution_corpus, content_transformer(tolower))

evolution_corpus = tm_map(evolution_corpus, removeNumbers)

evolution_corpus = tm_map(evolution_corpus, removePunctuation)

evolution_corpus = tm_map(evolution_corpus, removeWords, c("the", "and", stopwords("english")))

evolution_corpus = tm_map(evolution_corpus, stripWhitespace)

```
